#!/usr/bin/env bash

# Check if the named pipe exists
fifo="/tmp/stop_fifo"
if [ -p "$fifo" ]; then
    # Send a message to the named pipe to terminate the process
    echo "Stop" > "$fifo"
else
    echo "Named pipe not found"
fi
